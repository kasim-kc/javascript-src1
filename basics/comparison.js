// Comparing Different DataTypes should not happen in order to get predictive results

// console.log("3" > 2)
// console.log("3a" > 2)   // All of which depends on How JS converts to datatype for comparison

// console.log(null > 0)
// console.log(null == 0)
// console.log(null >= 0)     // The equality Check '==' and comparison operators work differently is JS

// '==' -> converts the datatype for comparison
// '===' -> compares the datatype as well     

